import React, { FC } from 'react';

interface TopBannerProps {
  title: string;
  scrollDownPath: string;
}

export const TopBanner: FC<TopBannerProps> = () => {
  return <div className="">TopBanner</div>;
};
